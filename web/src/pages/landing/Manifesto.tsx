/*
 * The one serif line. Zone: src/pages/landing/**.
 *
 * Instrument Serif italic, used exactly once on the whole site, on an inverted
 * paper band. Using it anywhere else dilutes it to nothing.
 *
 * STUB.
 */

export function Manifesto() {
  return (
    <section className="manifesto-band" data-surface="paper">
      <p className="manifesto">
        We lend money to AI workers and get paid back automatically with a slice of every penny
        they earn.
      </p>
    </section>
  );
}

export default Manifesto;