packages:
  - agents
  - web
allowBuilds:
  # esbuild ships its platform binary through a postinstall script. Vite needs it.
  esbuild: true
  bufferutil: false
  keccak: false
  utf-8-validate: false
